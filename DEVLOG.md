# יומן הפיתוח

רשומה לכל אבן דרך, לפי סדר העבודה. **אין כאן תאריכים — רק מה נעשה ולמה.**

---

## 1. שלד הפרויקט והגדרות

פרויקט גודו 4.7 נקי, כל הממשק בעברית, וכל הקיצורים מוגדרים ב-`project.godot`
ולא בקוד. **מסך עברית = בלי אמוג'י ואייקוני יוניקוד**: הפונט המובנה של גודו לא
מכיל אותם, והם מופיעים כריבועים ריקים. כל סמל בממשק הוא קובץ PNG.

## 2. העולם והמצלמה

רצפת הגריד מצוירת על משטח אמיתי בעולם, ולא ברקע — היא לא זזה כשהמצלמה זזה.
המצלמה עובדת כמו בלנדר: **גלגלת אמצעית** סיבוב סביב העצם הנבחר, **Shift + גלגלת**
הזזת נקודת ההסתכלות, **Ctrl + גלגלת** זום, **WASD / QE** תנועה, **F** מיקוד חזרה.

## 3. בנק האסטים

סורק לבד את `assets/models` — **שם התיקייה העליונה הופך לקטגוריה בעברית**, וכל
קובץ `.glb` הופך לכרטיס. תמונות ממוזערות נוצרות אוטומטית ממודל שעדיין לא קיים
להן תמונה. הבנק נפתח ונסגר מכפתור הפינה.

## 4. הצבה בגרירה ומצב רפאים

גוררים כרטיס מהבנק אל העולם: אובייקט רפאים שקוף עוקב אחרי העכבר על הרצפה,
ו-**Shift** מצמיד לרשת של חצי מטר. בשחרור העצם נוצר **במצב רפאים** — מרחף עם
תנועה עדינה ושקוף — כדי שאפשר יהיה למקם ולסובב אותו לפני שהוא "מתקבע".
אישור (או Esc / קליק על אוויר) מפיל אותו למקום באנימציה קצרה עם הקפצה.

## 5. בחירה וגיזמו

לכל עצם יש גוף התנגשות בגודל המודל בשכבה `props` — זה מה שמאפשר ללחוץ עליו
ולבחור אותו. הגיזמו מציע הזזה בשלושת הצירים, הזזה חופשית על הרצפה, וסיבוב
בשלוש טבעות. **קליק ימני באמצע גרירה מבטל** ומחזיר את העצם בדיוק למקום שממנו
יצא — כמו בבלנדר, וזו הדרך הבטוחה לטעות בלי לפחד.

## 6. הסרגל הצף

מעל העצם הנבחר, ועוקב אחריו: **הנח / שכפול / מחיקה**. במקום לזכור קיצורים.

## 7. עריכת גודל

ידיות גרירה בכל ציר משנות את **מידות המודל עצמו** — המודל נבנה מחדש, ולא נמתח
בסקייל. אחרת הטקסטורות היו נמתחות והעובי של הרגליים היה משתנה עם הגובה.

## 8. חומריות

חלון החומריות מציג שינוי בזמן אמת, אבל שומר רק באישור — **ביטול מחזיר את המצב
המדויק שהיה לפני הפתיחה**.

## 9. הכדורים

ארבעה כדורים — ים, רגל, סליים וברזל — מזוהים לפי שם האסט ולא לפי סדר. לכל כדור
**קוטר ברירת מחדל** משלו, כי קבצי ה-`.glb` יוצאים מבלנדר בקני מידה לא צפוי.
עיגול קנה המידה מגדיל את הכדור **סביב מרכזו** (למעלה, למטה ולצדדים במידה שווה),
וכדורי סליים ורגל מקבלים ידית נוספת למעיכה ומתיחה בעזרת shape keys. כדור הברזל
קשיח — אין לו את הידית הזו.

## 10. ציר הזמן — גרסה ראשונה

24 פריימים, ובכל פריים **התנוחה המדויקת** של כל עצם שהוקלט בו. סרגל הפריימים
מראה מסגרת זהובה לפריים הנוכחי ונקודה ירוקה לפריים שיש בו הקלטה, ולחיצה על
משבצת מעבירה את הציר לאותו פריים.

**החלטה מרכזית:** המנוע יושב בצומת נפרד ומדבר עם מנוע הבחירה והגיזמו רק דרך
ה-API הציבורי שלהם — הוא לא נוגע בהם מבפנים. כך אפשר להחליף את הממשק של הציר
מבלי לסכן את הבחירה, את הגיזמו ואת העריכה.

## 11. שדרוג ציר הזמן — אינטרפולציה

במצב **Stepped** הפריים מחזיק את ההקלטה האחרונה שלפניו (סטופ-מושן אמיתי).
במצב **Spline** הציר מעביר בין ההקלטות באופן רציף: סיבוב ב-**slerp**, ומיקום
וגודל ב-**lerp**. כשקיימות הקלטות משני צידי הקטע, המיקום עובר ל-**cubic_interpolate**
עם נקודות בקרה — תנועה בקשת ולא בקו שבור. בקצוות הציר (הקלטה בודדת, או קטע
שאין לו הקלטה שנייה) הוא נופל בחזרה להחזקה, כדי לא להמציא תנוחות שלא הוקלטו.

## 12. שדרוג ציר הזמן — בצל (Onion Skin)

רוח רפאים שקופה של **העצם הנבחר בלבד**: בתנוחה של הפריים הקודם ובתנוחה של
הפריים הבא, כדי לראות לאן התנועה נמשכת. הרוח היא שכפול של **המודל בלבד** —
בלי גוף ההתנגשות ובלי קבוצת העצים — כך שהיא **לא ניתנת לבחירה ולא משתתפת
בפיזיקה**, והיא מצוירת בחומר אחיד ללא תאורה.

הרוחות נבנות מחדש **רק כשהעצם הנבחר מתחלף**; מעבר בין פריימים רק מזיז אותן.
אחרת כל פריים בניגון היה בונה מחדש גיאומטריה — בזבוז כבד בזמן ניגון. הרוחות
נעלמות מיד עם ביטול הבחירה ועם כיבוי הבצל.

## 13. שדרוג ציר הזמן — קיצורי מקלדת

`←` / `→` פריים קודם והבא, `,` / `.` Keyframe קודם והבא, `I` לכידת פריים,
ו**רווח** נגן/עצור. הקיצורים מטופלים רק אם שום דבר אחר בממשק לא צרך את המקש,
כדי לא לגנוב מקשים מכפתור שממוקד.

**החלטה:** החצים עברו לניווט פריימים, ולכן **הסיבוב בחצים הוסר** — שני שימושים
לאותו מקש היו מתנגשים, וניווט בציר הוא השימוש התכוף יותר בעבודה על סרטון.

## 14. שדרוג ציר הזמן — צילום אוטומטי

כשהמתג דלוק, כל **סיום הזזה או סיבוב** של העצם הנבחר נקלט לפריים הנוכחי לבד —
בלי לחזור לכפתור הלכידה בין פריים לפריים. גרירה שבוטלה בקליק ימני **לא נקלטת**,
כדי שביטול יישאר ביטול.

## 15. שדרוג ציר הזמן — הממשק הגרפי

הלוח נבנה מחדש סביב סט האייקונים שהגיע (`assets/2DUI/TimelineIcons`): שורת כלים
של `TextureButton` במקום כפתורי טקסט, ומתחתיה הסרגל. כל מתג (Stepped/Spline, בצל,
צילום אוטומטי) מציג **ציור אחר** למצב דלוק ולמצב כבוי, כך שהכפתור תמיד מראה את
המצב שאליו תוביל הלחיצה.

**החלטה — סרגל אחד מתוח:** רצועת הפריימים מצוירת פעם אחת (288×201, 24 שנתות של
12px) ונמתחת על כל רוחב החלון, כך שכל פריים מקבל עמודה של רוחב÷24. הפריים ה-i
ממוקם ב-`x = (frame - 1) × frame_width`, ולכן השנתות המצוירות תמיד נופלות בין
העמודות. לחיצה על כל נקודה בסרגל בוחרת את הפריים שלה, וכך הלוח מתאים את עצמו לכל
גודל חלון — במקום רוחב קבוע עם גלילה אופקית.

**החלטה — שיקוף במקום קבצים כפולים:** לשני כפתורי "הקודם" (פריים ו-Keyframe) אין
נכס נפרד. שניהם משתמשים באותה תמונה בדיוק עם `flip_h`, ולכן אין קבצים כפולים
שעלולים להתעדכן בנפרד. אייקוני "הבא" הם אותו ציור משוקף בחזרה.

**החלטה — אייקון ה-Pause משמש כעצירה:** בסט הנכסים אין אייקון Stop נפרד, והמנוע
לא מכיר השהיה באמצע ניגון. לכן "עצירה" זהה להשהיה — הכפתור עוצר את הניגון
ונשאר על הפריים שבו עצרנו, ומכאן גם האייקון וגם הכפתור "חזרה לפריים הראשון"
(הריבוע) לצידו.

## 16. סדר בתיקיות הפרויקט

הפרויקט היה קבור בתוך שלוש קומות של `new-game-project` שהצטברו זו בתוך זו, ומאגר
ה-git ישב באחת מהן ולא בשורש. כל התוכן הועלה לקומה אחת: `project.godot` ומאגר
ה-git יושבים עכשיו **באותה תיקייה**, וכל הקומות המיותרות נמחקו. `Plans/` — תוכניות
העיצוב — נשאר לצד הפרויקט ומוחרג ב-`.gitignore`.

**מה נבדק אחרי ההעברה:** כל 105 הנכסים נטענים מהמקומות החדשים (אפס כשלים), 86
הבדיקות עוברות, ואין אף אזכור של הנתיב הישן באף קובץ של הפרויקט. `user://` לא זז,
כי שם הפרויקט לא השתנה — הפרויקטים השמורים של הילדים ממתינים ב-
`app_userdata/MyFirstStudio`. מטמון הייבוא (`.godot`) נזרק במכוון ונוצר מחדש בפתיחה,
כדי שלא יישארו בו נתיבים של המיקום הישן.

**תיקון צדדי:** `scripts/core/gizmo_ball_handles.gd` התייחס למחלקה `SimpleBall` שכבר
לא קיימת, והפיק שגיאת parse בכל פתיחת פרויקט. ההתייחסות הוסרה — הקובץ עצמו לא
בשימוש בשום מקום, והבדיקה המבנית (חיפוש המאש עם צורות המיזוג) נשארה כפי שהיא.